/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AutomatizacionWiscivService } from './automatizacion-wisciv.service';

describe('Service: AutomatizacionWisciv', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AutomatizacionWiscivService]
    });
  });

  it('should ...', inject([AutomatizacionWiscivService], (service: AutomatizacionWiscivService) => {
    expect(service).toBeTruthy();
  }));
});
